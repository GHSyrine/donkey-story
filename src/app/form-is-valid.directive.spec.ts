import { FormIsValidDirective } from './form-is-valid.directive';

describe('FormIsValidDirective', () => {
  it('should create an instance', () => {
    const directive = new FormIsValidDirective();
    expect(directive).toBeTruthy();
  });
});
